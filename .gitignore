# Python
__pycache__/
*.pyc
*.pyo

# Jupyter
.ipynb_checkpoints/

# Raw data and large outputs (tracked via .gitkeep only)
data/raw/**
!data/raw/.gitkeep
!data/raw/README.md
data/processed/structured_scenes.json
outputs/generated_images/**
!outputs/generated_images/.gitkeep
outputs/grids/**
!outputs/grids/.gitkeep
outputs/metrics.csv
outputs/pilot/

# Symlinks to Google Drive (local only)
outputs_drive
data_drive_processed

# Database
*.db

# Zip files
*.zip

# Submission folder (local only — PPTX and video script stay off GitHub)
submission/

# Screen recordings — tracked via Git LFS, not excluded
# *.mov and *.mp4 are handled by .gitattributes
*.avi
*.mkv
*.webm

# Model weights
*.safetensors
*.ckpt
*.pt
*.bin

# Dataset files
*.mat

# Secrets
.env

# Model cache
models_cache/

# OS noise
.DS_Store
Thumbs.db
