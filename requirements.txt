# ---------------------------------------------------------------
# Colab-managed packages — DO NOT reinstall via pip in Colab.
#
# torch / torchvision  Pre-installed on T4 (torch==2.2.0+cu121).
#   Reinstalling breaks the CUDA environment.
#
# numpy                Colab ships numpy 2.x and compiles pandas /
#   scipy / h5py against that ABI. Downgrading to 1.x causes a
#   "numpy.dtype size changed" binary-incompatibility error at import.
#   The lines below are for local / non-Colab environments only.
#   In Colab, skip all four lines:
#     torch==2.2.0
#     torchvision==0.17.0
#     numpy==1.26.4    <- also skip in Colab
#     pandas==2.2.2    <- also skip if already present
# ---------------------------------------------------------------
torch==2.2.0
torchvision==0.17.0

# Diffusion stack
diffusers==0.27.2
transformers==4.40.2
accelerate==0.30.1
safetensors==0.4.3

# ControlNet preprocessing
controlnet-aux==0.0.7

# Evaluation metrics
open-clip-torch==2.24.0
lpips==0.1.4
torchmetrics[image]==1.3.2

# Data loading
scipy==1.13.0
h5py==3.11.0

# Numerics and visualisation
# numpy — version intentionally unpinned; Colab provides numpy 2.x and the
#   compiled ABI must not be broken by a downgrade. Local envs get whatever
#   pip resolves. Pin explicitly here only for fully isolated reproducibility:
#   numpy==1.26.4  (numpy 1.x) or numpy==2.0.2 (numpy 2.x, needs pandas>=2.1)
numpy>=1.26.4
pandas==2.2.2
Pillow==10.3.0
matplotlib==3.8.4
seaborn==0.13.2
tqdm==4.66.4

# Config
pyyaml==6.0.1
